<% title t '.title' %>
<h1><%= t '.title' %></h1>

<p>
	<%= flash[:success] %>
</p>

<div class="panel borderless-top panel-default">
	<div class="panel-body">
		
		<p>Member since: <%= "#{[@registered_since.year, @registered_since.month, @registered_since.day].join '-'}" %></p>
		<strong><%= t :subjects, scope: 'activerecord.attributes.tutor' %>:</strong>
		<% @tutor.tag_list.each do |tag| %>
			<span class="label label-primary"><%= link_to tag, tag_path(tag) %></span>
		<% end %>
		<br />
		<strong><%= t(:description, scope: 'activerecord.attributes.tutor') %></strong> <%= @tutor.description%><br />
		<strong><%= t(:rate, scope: 'activerecord.attributes.tutor') %></strong> <%= "#{@tutor.currency} #{@tutor.rate}" %><br />
		<strong><%= t(:country, scope: 'activerecord.attributes.tutor') %></strong> <%= @tutor.country %><br />
		<strong>Address:</strong> <%= @tutor.address %><br />
		<p>
			<%= image_tag "http://maps.google.com/maps/api/staticmap?size=450x450&sensor=false&zoom=16&markers=#{@tutor.latitude}%2C#{@tutor.longitude}&language=#{locale.to_s}", :class => "img-thumbnail", :alt => "#{t('.location_img')}" %>
		</p>	
		<p>
			<%= t(:education, scope: 'activerecord.attributes.tutor') %>
		</p>
		<%= render partial: 'table_educational_experiences', locals: {educational_experiences: @educational_experiences} %>
		<%= render partial: 'table_languages', locals: {languages: @languages} %>
	</div><!-- End Panel-body -->
</div><!-- End Panel -->